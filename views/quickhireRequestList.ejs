<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Request List Quick Hire - Jobster</title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css"
    />
    <link rel="shortcut icon" href="/images/icon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta
      name="keywords"
      content="Soft Responsive web template, Bootstrap Web Templates, Flat Web Templates, Android Compatible web template,
Smartphone Compatible web template, free webdesigns for Nokia, Samsung, LG, SonyEricsson, Motorola web design"
    />
    <script>
      addEventListener(
        "load",
        function() {
          setTimeout(hideURLbar, 0);
        },
        false
      );
      function hideURLbar() {
        window.scrollTo(0, 1);
      }
    </script>
    <!--//tags -->
    <link
      href="/css/bootstrap.css"
      rel="stylesheet"
      type="text/css"
      media="all"
    />
    <link href="/css/style.css" rel="stylesheet" type="text/css" media="all" />

    <link href="/css/font-awesome.css" rel="stylesheet" />
    <!-- //for bootstrap working -->
    <link
      href="//fonts.googleapis.com/css?family=Work+Sans:200,300,400,500,600,700"
      rel="stylesheet"
    />
    <link
      href="//fonts.googleapis.com/css?family=Lato:400,100,100italic,300,300italic,400italic,700,900,900italic,700italic"
      rel="stylesheet"
      type="text/css"
    />
  </head>
  <body>
    <!-- header -->

    <% include partials/navbarCompany %>

    <hr />

    <div class="container">
      <div class="quickhire-wrapper min-height-content">
        <div class="row">
          <div class="col-md-8 col-sm-7 col-xs-12">
            <h1>List of Person</h1>
          </div>
        </div>
        <hr />

        <ul class="list-group">
          <% if(request_list.length > 0) {%> <% for(i = 0; i <=
          request_list.length-1 ; i++) {%> <% if
          (quickhire.request_list.filter(list => list.seeker_id ==
          request_list[i].id).length === 0) { %>
          <li class="list-group-item quickhire-list">
            <div class="row">
              <form
                method="post"
                action="/company/quickhire/<%= quickhire.id %>/request/<%= request_list[i].id %>"
              >
                <div class="col-md-2 col-sm-3 col-xs-4">
                  <img
                    class="img-circle img-responsive"
                    src="<%= request_list[i].userDetail.profile_image %>"
                  />
                </div>
                <div class="col-md-8 col-sm-7 col-xs-6">
                  <h3 style="text-transform: capitalize;">
                    <a href="/profile/<%= request_list[i].id %>"
                      ><%= request_list[i].name %></a
                    >
                  </h3>
                  <p style="font-size: 17px;">
                    <i class="fa fa-map-marker"></i>
                    <%= request_list[i].userDetail.location %>
                  </p>
                </div>
                <div class="col-md-2 col-sm-12 col-xs-12">
                  <input
                    type="submit"
                    class="btn btn-primary btn-block btn-hire"
                    value="Hire"
                  />
                </div>
              </form>
            </div>
          </li>
          <% } %> <% } %> <% } else { %>
          <li
            class="list-group-item"
            style="text-align: center; font-size: 16px;"
          >
            It seems like no person are found as your requirement
          </li>
          <% } %>
        </ul>
      </div>
    </div>

    <!-- footer -->
    <% include partials/footer %>
    <!-- //footer -->

    <a href="#home" class="scroll" id="toTop" style="display: block;">
      <span id="toTopHover" style="opacity: 1;"> </span
    ></a>
    <!-- js -->
    <script type="text/javascript" src="js/jquery-2.1.4.min.js"></script>

    <script type="text/javascript" src="js/bootstrap.js"></script>
  </body>
</html>
